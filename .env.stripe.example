# =====================================================
# STRIPE CONNECT - ENVIRONMENT VARIABLES
# =====================================================
# Copie este arquivo para .env.local e preencha os valores

# -----------------------------------------------------
# STRIPE API KEYS
# -----------------------------------------------------
# Get from: https://dashboard.stripe.com/test/apikeys
# Production keys: https://dashboard.stripe.com/apikeys

# Secret Key (Backend only - NEVER expose in frontend)
STRIPE_SECRET_KEY=sk_test_your_secret_key_here

# Publishable Key (Frontend safe - can be public)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# -----------------------------------------------------
# STRIPE WEBHOOKS
# -----------------------------------------------------
# Get from: https://dashboard.stripe.com/test/webhooks
# Production: https://dashboard.stripe.com/webhooks
# Endpoint: https://yourdomain.com/api/stripe/webhooks

STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# -----------------------------------------------------
# SUPABASE
# -----------------------------------------------------
# Get from: https://app.supabase.com/project/_/settings/api

# Supabase URL (Public)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase Anon Key (Public - safe for frontend)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Supabase Service Role Key (Backend only - NEVER expose)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# -----------------------------------------------------
# APPLICATION URLS
# -----------------------------------------------------
# Production: Use your actual domain
# Development: Use localhost

# Base URL for the application
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Onboarding redirect URLs
NEXT_PUBLIC_STRIPE_ONBOARDING_REFRESH_URL=http://localhost:3000/onboarding/refresh
NEXT_PUBLIC_STRIPE_ONBOARDING_RETURN_URL=http://localhost:3000/onboarding/complete

# -----------------------------------------------------
# OPTIONAL: STRIPE ADVANCED SETTINGS
# -----------------------------------------------------

# Enable Stripe CLI for local webhook testing
# STRIPE_CLI_WEBHOOK_SECRET=whsec_local_secret_from_stripe_listen

# Custom Stripe API version (default: latest)
# STRIPE_API_VERSION=2024-12-18.acacia

# -----------------------------------------------------
# NOTES
# -----------------------------------------------------
# 1. Never commit .env.local to git
# 2. Keep test and production keys separate
# 3. Rotate keys regularly in production
# 4. Use Stripe CLI for local development:
#    stripe listen --forward-to localhost:3000/api/stripe/webhooks
